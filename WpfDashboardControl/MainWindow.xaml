<Window
    x:Class="WpfDashboardControl.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:WpfDashboardControl.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="1000"
    Height="750"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <controls:DashboardHost
            Grid.Row="0"
            EditModeEnabled="{Binding CommandEditModeEnabled}"
            EditingComplete="{Binding CommandEditingComplete}"
            ItemsSource="{Binding Widgets}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Command="{Binding CommandAddOneByOne}" Content="Add One By One" />
            <Button Command="{Binding CommandAddOneByTwo}" Content="Add One By Two" />
            <Button Command="{Binding CommandAddTwoByOne}" Content="Add Two By One" />
            <Button Command="{Binding CommandAddTwoByTwo}" Content="Add Two By Two" />
            <Button Command="{Binding CommandRemoveLastElement}" Content="Remove Last One" />
        </StackPanel>
    </Grid>
</Window>
